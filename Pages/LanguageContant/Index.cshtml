@page
@model dotnetCore.Pages.LanguageContant.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LanguageContant[0].Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LanguageContant[0].Abbreviation1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LanguageContant[0].Abbreviation2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LanguageContant[0].Connection)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LanguageContant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Abbreviation1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Abbreviation2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Connection)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
